// feedback-api local helpers. BUNDLE is the idempotency route-key namespace
// (mirrors invoicing-api/_helpers.ts). Handlers import requireCap directly
// from _shared/handler-helpers.ts.

export const BUNDLE = 'feedback-api';
