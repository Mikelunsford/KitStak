# PROJECT.md

Filled-in agent configuration for Kitstak. Generated by the Project Init agent on 2026-05-18 against the repo at `C:\Users\Mike Lunsford\Desktop\KitStak v.01`.

Inline comments record the detection source and confidence so future maintainers can re-verify each field. Anything marked `n/a` means no signal was detected and the field is intentionally skipped.

```yaml
project:
  name: "Kitstak"                                                      # source: CLAUDE.md product name (package.json#name="kitstak-app"); confidence: high
  short_code: "KSTK"                                                   # source: user-supplied
  description: "Operating system for SMB 3PLs, manufacturers, and co-pack/ecom operations." # source: package.json + CLAUDE.md; confidence: high

stack:
  frontend: "React 18 + Vite + TanStack Query"                         # source: apps/web/package.json; confidence: high
  backend: "Supabase Edge Functions (Deno)"                            # source: supabase/functions/; confidence: high
  database: "Supabase Postgres"                                        # source: supabase/migrations/; confidence: high
  hosting: "Vercel (us-west-1)"                                        # source: vercel.json + 00-canon/01-architecture.md; confidence: high
  auth: "Supabase Auth (JWT + TOTP MFA, SSO/SAML for Enterprise)"      # source: 00-canon/01-architecture.md; confidence: high
  testing: "Vitest + Playwright + @axe-core/playwright"                # source: apps/web/package.json; confidence: high
  ci: "GitHub Actions"                                                 # source: .github/workflows/; confidence: high
  mobile: "n/a"                                                        # source: no mobile signals
  analytics_provider: "n/a"                                            # source: no signals
  observability_sink: "n/a"                                            # source: no @sentry/* etc.
  load_tool: "n/a"                                                     # source: no signals
  bundle_tool: "size-limit (40 kB gzip on index chunk)"                # source: apps/web/.size-limit.cjs + 00-canon/01-architecture.md; confidence: high
  design_system_stack: "Tailwind + hand-rolled primitives + CSS-variable tokens" # source: 00-canon/01-architecture.md; confidence: high

paths:
  repo_root: "."
  shared_context: "/CLAUDE.md"                                         # source: constitution lives in CLAUDE.md, not /docs/CONTEXT.md
  agents_dir: "/CLAUDE AGENTS"
  api_contracts: "/docs/api"                                           # source: docs/api/{crm,finance,identity,inventory,ops,sales,vendors,cross_cutting}.md
  db_schemas: "/supabase/migrations"
  migrations: "/supabase/migrations"
  tests: "/apps/web/src (Vitest) + /apps/web (Playwright)"             # source: apps/web/package.json scripts + playwright.config.ts
  docs: "/docs"
  journal: "/03-workspace/journal"                                     # source: CLAUDE.md mandates this path; confidence: high
  escalations: "n/a"                                                   # source: no folder yet; create on first use under 03-workspace/journal/escalations
  questions: "n/a"                                                     # source: no folder
  blueprint: "/00-canon"                                               # source: 00-canon/01-architecture.md is the canon; confidence: medium
  deploy: "n/a"                                                        # source: no /deploy dir; deploys via .github/workflows/deploy-*.yml + Vercel
  ci_dir: "/.github/workflows"
  design_system: "/apps/web/src/components/ui"                         # source: 00-canon/01-architecture.md folder structure; confidence: high
  whitelabel_lib: "n/a"                                                # source: single product, multi-tenant via org_id
  plugins: "n/a"                                                       # source: no /plugins folder; "plugin bundle" in CLAUDE.md = feature-flag SKUs, not code
  types_file: "/apps/web/src/lib/database.types.ts"                    # source: root package.json#scripts.gen:types; confidence: high

reference_docs:
  architecture: "/00-canon/01-architecture.md"                         # source: present; canon
  build_order: "n/a"                                                   # source: wave plan lives in 03-workspace/journal/wave-*.md, no single build-order doc
  code_style: "n/a"                                                    # source: encoded in CLAUDE.md + ESLint no-restricted-imports
  naming_conventions: "n/a"                                            # source: encoded in CLAUDE.md (e.g., _cents suffix, NNNN_snake_case migrations)
  comms_protocol: "n/a"
  glossary: "n/a"
  dx_testing: "/docs/operations/probes.md"                             # source: probes doc covers cross-tenant RLS test discipline; confidence: medium

phasing:
  model: "Waves"                                                       # source: CLAUDE.md + 03-workspace/journal/wave-*.md; confidence: high
  phases:
    - "Wave 0 — Foundation"
    - "Wave 1 — Identity, Tenancy, Branding"
    - "Wave 2 — Domain Ports"
    - "Wave 3 — Integration"
    - "Wave 5 — Probes (cross-tenant RLS)"
    - "Wave 6 — Customer Zero"
    - "Phase 6 — Chassis (in progress)"                                # source: latest commit f97bc24

conventions:
  branch_pattern: "claude/<slug>"                                      # source: user-confirmed 2026-05-18. Matches 100% of git history; wave/domain provenance lives in commit messages and PR titles rather than branch names. Revisit if non-Claude-Code authors start dispatching.
  commit_style: "Wave-prefixed (`Wave N: <subject>`, `Wave N hotfix N: <subject>`, `docs: <subject>`)" # source: git log -25; not strict Conventional Commits
  money_handling: "integer cents in BIGINT; _cents column suffix; roundHalfEven; bigintReplacer for wire; currency snapshotted at issuance" # source: CLAUDE.md
  tenant_model: "multi-tenant via org_id + RLS (Patterns A/B/C); RLS filters never throws; 404 for cross-tenant" # source: CLAUDE.md
  api_envelope: "{ data, error }"                                      # source: apps/web/src/lib/apiClient.ts (INVALID_ENVELOPE validation)
  state_pattern: "server state in TanStack Query (staleTime 30s, refetchOnWindowFocus false, retry 1); UI state in React Context + useState; forms via useState + Zod safeParse" # source: CLAUDE.md

quality_targets:
  a11y_target: "WCAG 2.1 AA"                                           # source: @axe-core/playwright + .github/workflows/lighthouse.yml; confidence: high
  coverage_floor: "80%"                                                # source: user-supplied (no vitest coverage threshold configured today — enforce when added)
  perf_budgets: "Bundle: 40 kB gzip on index chunk (size-limit). TTI/LCP: TBD — agree before Phase 6 close." # source: 00-canon/01-architecture.md (bundle); TTI/LCP not yet set
  themes: "light, dark"                                                # source: user-supplied
  compliance_regimes: "n/a"                                            # source: user deferred

tooling:
  types_regen_command: "pnpm gen:types"                                # source: root package.json#scripts; confidence: high

policy:
  banned_dependencies:                                                 # source: CLAUDE.md "What we refuse" + 00-canon/01-architecture.md + ESLint no-restricted-imports
    - "antd, @ant-design/*"
    - "@radix-ui/*, shadcn"
    - "redux, @reduxjs/toolkit, zustand, jotai, recoil"
    - "react-hook-form, formik"
    - "dayjs, date-fns, moment"
    - "lodash"
    - "axios"
    - "uuid (package)"
    - "next, @remix-run/*, gatsby"
    - "stock photography"
  required_dependencies:                                               # source: apps/web/package.json deps + 00-canon/01-architecture.md
    - "@supabase/supabase-js"
    - "@tanstack/react-query"
    - "react-router-dom v6"
    - "zod"
    - "lucide-react"
    - "sonner"
    - "tailwindcss"
  custom_rules:                                                        # source: CLAUDE.md non-negotiables
    - "Money: BIGINT cents + _cents suffix + roundHalfEven; _shared/money.ts byte-identical to apps/web/src/lib/money.ts (money.parity.test.ts contract)."
    - "RLS: every tenant-scoped table from migration 0001; filters never throws; cross-tenant reads → 200 []; cross-tenant workflow POSTs → 404; plugin bundle gates → 404; per-route feature-flag misses → 403 FEATURE_DISABLED { flag }."
    - "Migrations: forward-only, NNNN_snake_case.sql, never edit after apply, multi-stage drops, idempotent DDL (IF EXISTS / IF NOT EXISTS); every migration header declares Wave/Phase/Closes/DOWN MIGRATION/date/constitutional alignment."
    - "Zod canon: _shared/types.ts byte-identical to apps/web/src/lib/types.ts (pnpm test:contract); drift is a release blocker."
    - "Idempotency-Key (UUID v4) required on every non-GET; RFC 8785 canonical body hash; 409 IDEMPOTENCY_CONFLICT on collision; nightly GC of rows older than 7 days."
    - "audit_log append-only via RLS (deny UPDATE/DELETE/INSERT for authenticated; service-role-only writes); hash chain active from migration 0001; auto-state-transition triggers, no best-effort writes."
    - "Capabilities: 8 roles, ~120 caps in <domain>.<resource>.<action> shape; requireCap(caller, cap) on every state-changing handler; 403 FORBIDDEN if denied; SPA mirrors policy for button-hiding only — server is authority."
    - "Wave-based delivery: each wave declares scope/deliverables/risks closed/risks carried/constitutional invariants verified; closeout journal at 03-workspace/journal/wave-<N>-<slug>.md."
    - "Risk IDs: R-W<wave>-<seq>, R-W<wave>-<DOMAIN>-<seq>, R-W<wave>-CO-<seq> for carries; follow-ups in F-Wave<N>-<seq>."
    - "User-facing copy: no em dashes, no double hyphens, no emojis, no stock photography, no generic gradients."
    - "Branding: navy #0a1628, ink #f5f1e8, accent #c8102e; Bebas Neue (display) / Inter Tight (body) / JetBrains Mono (code)."
```

## Re-running

Safe to re-run. If `PROJECT.md` already exists, the Project Init agent will diff its new draft against this file, show changes, and ask before overwriting. The previous file would be saved as `PROJECT.md.bak`.

## Open items / recommended follow-ups

- **`perf_budgets` TTI/LCP**: settle target numbers before Phase 6 close so the lighthouse workflow can gate against them.
- **`branch_pattern`**: `claude/<slug>` is currently the only pattern in git history. If human-authored work should use a different pattern (e.g. `<phase>/<domain>/<slug>` per the template example), override this field manually.
- **`escalations` / `questions` folders**: create them on first use rather than now.
- **Reference docs** (`code_style`, `naming`, `comms`, `glossary`, `build_order`): currently `n/a` because the constitution covers them. If agents start asking, promote relevant CLAUDE.md sections into stub files under `/docs/`.
- **Next step per the kit**: run the Kit Validator agent to confirm all paths resolve.
