# canon-steward-check allowlist
# F-Wave7-CANON-STEWARD-01
#
# Format: <rule>|<file>|<snippet>
#   rule    = marker | orphan-link | orphan-route
#   file    = repo-relative path (forward slashes, no leading ./)
#   snippet = the trimmed first 160 chars of the offending line, OR the route
#             path itself for orphan-route entries
#
# Line numbers are intentionally NOT load-bearing: line drift must not cause
# a previously-allowed entry to silently fall back into failing CI.
#
# Every entry below MUST cite the follow-up that will close it. STATUS.md
# tracks the open follow-ups; an item is gone from this file the moment the
# follow-up lands and the violation is gone from the tree.
#
# DO NOT add new entries without a follow-up ID and a one-line reason.

# ---------------------------------------------------------------------------
# orphan-route: sales-config sub-pages and the value-added services catalog.
# These six routes are deep settings reachable from the items / quotes /
# admin surfaces rather than from the primary Sidebar. They are NOT silent
# orphans of the PR #27 / PR #34 class because no `<Link to="..."/new">`
# points at a missing /new route; the static route is registered and the
# detail surfaces link to them. The Sidebar IA intentionally does not
# surface every config sub-page.
# Follow-up: F-Wave7-SIDEBAR-IA-01 (decide whether sales-config and VAS
# should get their own collapsible Sidebar section in Phase 8 or remain
# linked from the admin / items surfaces only).
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/settings/sales-config/taxes
orphan-route|apps/web/src/routes.ts|/settings/sales-config/currencies
orphan-route|apps/web/src/routes.ts|/settings/sales-config/exchange-rates
orphan-route|apps/web/src/routes.ts|/settings/sales-config/payment-methods
orphan-route|apps/web/src/routes.ts|/settings/sales-config/pricing-tiers
orphan-route|apps/web/src/routes.ts|/catalog/vas

# ---------------------------------------------------------------------------
# orphan-route: /imports/history.
# Reachable from the Imports wizard (/imports), which is in the Sidebar
# Tools section. Sidebar surfaces /imports and the wizard surfaces a link
# to history. Not a true orphan.
# Follow-up: F-Wave7-SIDEBAR-IA-01 (same as above; either promote to
# Sidebar or drop from this allowlist when the IA decision lands).
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/imports/history

# ---------------------------------------------------------------------------
# orphan-route: /search.
# Global search is a top-bar feature reachable from AppShell's header,
# not a Sidebar entry. The route is intentionally NOT in SIDEBAR_MODES
# because it would compete with the workflow-stage IA without adding
# discoverability value (operators reach it via the header search box).
# Follow-up: F-Wave9-SEARCH-PROMOTE-01 (revisit if usage data shows the
# header search box is being missed).
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/search

# ---------------------------------------------------------------------------
# orphan-route: /3pl-operations/production (and /new).
# Legacy production-runs route. Replaced by /manufacturing/runs in UX-Q1
# (PR #112). Kept registered as a `<Navigate>` redirect so old bookmarks
# land on the new form. Not in the sidebar by design.
# Follow-up: F-Wave9-LEGACY-PRODUCTION-ROUTE-RETIRE-01 — drop the
# redirect once we're confident no bookmarks land on it.
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/3pl-operations/production
orphan-route|apps/web/src/routes.ts|/3pl-operations/production/new

# ---------------------------------------------------------------------------
# orphan-route: /account/security and /auth/recovery.
# Both routes are intentional orphans by design and must not appear in the
# sidebar.
#   /account/security is accessed from the Topbar profile dropdown menu
#     (under "Account security", above "Sign out"). Per-user surface, not
#     part of the work navigation.
#   /auth/recovery is a public landing page that consumes a Supabase
#     recovery token from the URL hash. Reached only by clicking the link
#     in the password-reset email. Should never be browsable.
# Shipped in F-Wave9-INVITE-PASSWORD-SETUP-01 (PR #150).
# No follow-up needed: both routes are stable orphan-by-design.
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/account/security
orphan-route|apps/web/src/routes.ts|/auth/recovery

# ---------------------------------------------------------------------------
# orphan-route: /no-active-org.
# F-Wave9-COWORK-SMOKE-03 hard-error surface for authenticated sessions
# whose JWT lacks the kitstak_org_id claim. ProtectedRoute renders this
# page inline when the claim is missing; the standalone route exists so
# the surface is deep-linkable for support flows. No sidebar entry by
# design: callers never browse here, they land here from the guard.
# Shipped in F-Wave9-COWORK-SMOKE-03.
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/no-active-org

# ---------------------------------------------------------------------------
# orphan-route: pillar landing pages (/manufacturing, /copack, /kitforce).
# Each pillar exposes a landing surface that orients the operator with live
# counts and entry-point tiles into that pillar's sub-routes. The Sidebar IA
# (UX-Q1 job-mode nav) surfaces the workflow-stage sub-routes (e.g.
# /kitforce/members, /copack/orders, /manufacturing/runs), not the pillar
# root, because the root competes with the workflow grouping without adding
# discoverability. Operators reach the landing pages from dashboard pillar
# tiles and from in-pillar breadcrumbs, not from the sidebar. Each root is
# plugin-gated (requiresPlugin) so orgs without the pillar flag never render
# it. Orphan-by-design, mirroring the existing pillar-home pattern.
# Follow-up: F-Wave9-SIDEBAR-PILLAR-ROOT-01 (decide whether pillar landing
# pages should get a sidebar "home" affordance once the job-mode IA settles).
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/manufacturing
orphan-route|apps/web/src/routes.ts|/copack
orphan-route|apps/web/src/routes.ts|/kitforce

# ---------------------------------------------------------------------------
# orphan-route: /manufacturing/runs/from-bom.
# Deep-link-only run-create surface that pre-fills a manufacturing run from a
# selected BOM (M2). Reached from the BOM detail page's "Start run from BOM"
# action and from the manufacturing-runs list, not from the sidebar. The
# canonical sidebar entry is /manufacturing/runs; this is a specialized
# create flow layered on it.
# Follow-up: F-Wave9-MFG-FROM-BOM-IA-01 (fold into the runs create surface
# once the BOM-to-run flow is validated with the first operator).
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/manufacturing/runs/from-bom

# ---------------------------------------------------------------------------
# orphan-route: spine plus add-ons re-route legacy redirects.
# The white paper V2 re-route moves spine and shared-building-block surfaces
# out of the plugin-gated /3pl-operations/* namespace to neutral ungated
# roots (/quotes, /projects, /purchasing/*, /catalog/*, /inventory/*,
# /settings/*). Each old list path stays registered as a SpineMoveRedirect
# (isRedirect) so existing bookmarks and emailed links still resolve. The
# new homes ARE in the sidebar; these old paths are redirect-only and
# intentionally absent from it. The /new and /:id variants are auto-exempt.
# Follow-up: F-Wave10-SPINE-REROUTE-REDIRECT-RETIRE-01 — drop the redirects
# once analytics confirm no live bookmarks land on the old paths.
# ---------------------------------------------------------------------------
orphan-route|apps/web/src/routes.ts|/3pl-operations/warehouses
orphan-route|apps/web/src/routes.ts|/3pl-operations/stock/levels
orphan-route|apps/web/src/routes.ts|/3pl-operations/stock/movements
orphan-route|apps/web/src/routes.ts|/3pl-operations/items
orphan-route|apps/web/src/routes.ts|/3pl-operations/boms
orphan-route|apps/web/src/routes.ts|/3pl-operations/vas
orphan-route|apps/web/src/routes.ts|/3pl-operations/sales-config/taxes
orphan-route|apps/web/src/routes.ts|/3pl-operations/sales-config/currencies
orphan-route|apps/web/src/routes.ts|/3pl-operations/sales-config/exchange-rates
orphan-route|apps/web/src/routes.ts|/3pl-operations/sales-config/payment-methods
orphan-route|apps/web/src/routes.ts|/3pl-operations/sales-config/pricing-tiers
orphan-route|apps/web/src/routes.ts|/3pl-operations/vendors
orphan-route|apps/web/src/routes.ts|/3pl-operations/purchase-orders
orphan-route|apps/web/src/routes.ts|/3pl-operations/vendor-bills
orphan-route|apps/web/src/routes.ts|/3pl-operations/expenses
